since with context.md introduction we write down user input there as goal, i suggest to remove input
since it's redundant. amend command will update this goal seaction. but rename it as "User Prompt"
section
