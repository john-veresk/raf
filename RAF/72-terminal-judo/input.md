during plannig mode if i resize terminal - layout of claude/codex output is messed up. it's not
happen if i run harness natively, without out raf layer. can we fix raf plan terminal resizing
layout mess somehow?

