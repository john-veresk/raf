context.md, during execution agent should add outcome file path in project files on completed.
remove ## Current State section entirely form all promts from context.md

---

Planning clarifications:
- `## Current State` should be explicitly forbidden in `context.md` guidance.
- On successful task completion, the execution prompt should require adding the current task's outcome path to `## Project Files`.
